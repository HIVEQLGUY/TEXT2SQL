# 长期前置上下文

更新时间：2026-07-19

## 长期方向

项目后续围绕：

```text
预策数据接入层 -> ClickHouse 数仓处理/建模验证
```

不再围绕旧 Text2SQL、DataAgent、自研接口接入、Doris、旧 RDS、DataX-Web 或 SeaTunnel 演示栈推进。

## 协作原则

- 先清理历史噪声，再推进新方向。
- 新增事实写入 `STATE-CURRENT.md`。
- 长期原则写入本文件或根目录 `AGENTS.md`。
- 旧接口经验只在需要参考时查看桌面留存包，不让旧链路重新成为默认假设。
- 建模验证以 ClickHouse 为准；旧 Doris 结果只能作为历史线索。

## 文档原则

- 文档保持短、当前、可执行。
- 不再新增长 handoff、checkpoint、临时总结。
- 已过期文档可以删除；有价值的历史经验应提炼为简短当前说明。
